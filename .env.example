# PostgreSQL connection string for Prisma and application services
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# JWT secret and other security-critical values should be set in deployment secrets
JWT_SECRET="replace-me"

# Frontend API base (used by Next.js)
NEXT_PUBLIC_API_BASE_URL="http://localhost:3001"
